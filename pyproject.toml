[project]
name = "crest-experiment"
version = "0.1.0"
description = "Refactored CREST experimental suite"
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.25.0",
    "torch>=1.9.0",
]
